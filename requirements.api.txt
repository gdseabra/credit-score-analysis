# Dependências mínimas para a API REST (sem Airflow, sem torch)
# Usadas pelo Dockerfile.api para manter a imagem leve.

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# Autenticação
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# MLflow / Databricks
mlflow==2.14.3
databricks-sdk>=0.20.0

# AWS (registro de modelos S3)
boto3>=1.34.0

# ML / Pipeline
scikit-learn==1.5.1
lightgbm==4.3.0
joblib==1.4.2
dill
numpy==1.26.4
scipy==1.13.1  # pinned: sklearn/shap pull scipy transitively; newer releases drop py3.11 wheels
pandas==2.2.2
pyarrow==15.0.2

# Explicabilidade
shap==0.45.1
numba==0.60.0  # pinned: shap pulls numba transitively; newer releases drop py3.11 wheels
anthropic>=0.49.0
